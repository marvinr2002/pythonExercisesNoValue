Data_1 = input("Give me a text\n")
it = iter(Data_1)
print(*it)

flash = ['jay garrick', 'barry allen', 'wally west', 'bart allen']
# Print each list item in flash using a for loop

for key in flash:
    print(key)      